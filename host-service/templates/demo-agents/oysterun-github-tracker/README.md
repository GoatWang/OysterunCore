# Oysterun GitHub Tracker Demo

This demo agent folder is safe to copy into a local browse root. It gives new users a small, self-contained project to inspect before starting their first Oysterun session.

Suggested first prompt:

```text
Review this project folder and propose a lightweight GitHub issue tracking workflow.
```

The template does not include credentials, tokens, private repository names, or Owner-local paths.
